# v2.15 Windows 正式验证与 E1P 数据解封

- **项目版本**：v2.15
- **日期**：2026-08-07
- **Git 边界**：`bbe728d..1f84b01`
- **状态**：CLOSED — Windows 正式三次 8/8、资源门全绿、E1P 五项数据全部解封
- **Tag**：无匹配 tag

## 概述

v2.15 Mac 全量开发（2026-08-06 合入 `bbe728d`）后，在 Windows 上完成正式路径验证和
研究数据准入。本日工作跨越 WP1-D（Windows 稳定性验收）和 WP1-E1P（数据能力解封）
两个工作包，M4 和 M3 两个里程碑均取得关键进展。

## WP1-D：Windows 正式稳定性验收

### 五个 Windows 缺陷修复

| commit | 缺陷 | 现象 | 修复 |
|---|---|---|---|
| `6a7f883` | supervisor 祖先链 | venv stub 阻断 worker 启动，`_run_cli` 返回 2 | psutil 祖先链检查替代 `getppid()` 严格相等 |
| `6ec2de3` | asyncio 进程退出 | `main()` 完成后 worker 进程悬挂，720s watchdog 强杀 | `sys.platform=="win32"` 时 `os._exit()` 强制退出 |
| `1bc3361` | evidence 身份比对 | `retrieved_at` 时间戳差异导致全局 archive 永远 mismatched | 比对改为 content_sha256 等身份字段，排除时间戳 |
| `62730ec` | mappingproxy 序列化 #1 | `phase_state` trace hash `json.dumps` 崩溃 | `_json_safe` 归一化非 dict Mapping → dict |
| `62730ec` | mappingproxy 泄漏 #2 | report 成功路径 announcement result 含 MappingProxyType → summary 序列化崩溃 | `extension.py` `_thaw_json` + 兜底 `_json_safe` |

### 修复链辅助

| commit | 内容 |
|---|---|
| `8e690f5` | `loop.run_until_complete` 替代 `asyncio.run`（绕开 shutdown_asyncgens 阻塞） |
| `2317ddc` | 尾部 phantom tool call 过滤（8 阶段完成后忽略 LLM 散发调用） |
| `2c65c11` | 聚合器 identity 改为比对新窗口 key（同参数窗口 = 同 snapshot，不要求 market 字节一致） |

### 三次 formal 结果

| session | elapsed | 阶段 | exit | quality |
|---|---|---|---|---|
| `115235` | 98.9s | 8/8 | REAL_EXIT=0 | quality_passed=True |
| `122418` | 105.1s | 8/8 | REAL_EXIT=0 | quality_passed=True |
| `122852` | 74.7s | 8/8 | REAL_EXIT=0 | quality_passed=True |

全部 12 tool calls、0 errors、三金融角色无越权。

### 资源门

| 指标 | 实测 | 门禁 | 结果 |
|---|---|---|---|
| P95 duration | 105.1s | ≤120s | ✅ |
| 峰值 RSS | 575.09MB | ≤600MB | ✅ |
| concurrency | 1 | ≥1 | ✅ |
| token 降幅 | 91.3%（vs 1,204,831） | ≥50% | ✅ |

### 测试

111 聚焦测试通过（validator + replay + resource + orchestration + snapshot + 新增回归）。
Ruff、pycompile、scope-check 全部通过。

## WP1-E1P：研究数据能力准入

E1P 要求五项能力全部 AVAILABLE 才能启动 E2 策略池：

| 能力 | 数据源 | 规模 | commit |
|---|---|---|---|
| CANONICAL_CALENDAR | SSE/SZSE 官方日历（原有） | — | 原有 |
| PIT_CORPORATE_ACTION | baostock `query_dividend_data` | 347 行 × 49 股 × 2020-2025 | `9ffcc17` |
| E0_FACTOR_SNAPSHOT | baostock qfq `query_history_k_data_plus` | 77,541 行 × 49 股 × 2020-2026 | `de5a618` |
| OFFICIAL_FORWARD_LABEL | calendar + qfq 计算 | 604 成熟决策日 | `4187062` |
| PIT_SECTOR | 赛题 Excel 6 板块 | STATIC_V1（无历史版本链） | `1f84b01` |

每项归档包含 SHA-256 pin 的 CSV + `source_records.json`，位于
`jiuwenswarm/evaluation/research_evidence/`。

### PIT_SECTOR 标注

赛题 6 板块为 STATIC_V1，标注 `STATIC_COMPETITION_SECTORS_NO_HISTORICAL_VERSION_CHAIN`。
若后续获得历史行业分类变更证据，可升级为 PIT_SECTOR_V2。

## WP1-E2 基线冻结

E1P 全绿后立即冻结 E2 基线（基准 `1f84b01`）。6 槽位策略池：
production_six_factor / t2_comparator / trend_short / trend_medium / trend_long /
similar_market_blend。Claude 待实现。

## 里程碑状态

| 里程碑 | 状态 |
|---|---|
| M0 事实一致 | ✅ CLOSED |
| M4 正式稳定性 | ✅ CLOSED |
| M3 动态研究 | E1P CLOSED，E2 基线冻结 |
| M1 Agent 决策 | LOCAL_IMPLEMENTED |
| M2 公告证据 | LOCAL_IMPLEMENTED |
| M5 正式提交 | BLOCKED_EXTERNAL |

## 讨论归档

完整 Codex/Claude 交接讨论见 [v2.15_2026-08-07_discussion.md](v2.15_2026-08-07_discussion.md)
（1505 行）。

## 已知限制

1. Windows CRLF（`core.autocrlf=true`）导致部分 pin 文件 worktree hash 与 git blob
   不符，`source_bytes_verified` 在 Windows 无法为 True。非本次引入，留待独立基建任务。
2. CSI300 benchmark 仅 2024-06 起，E2 similar_market_blend 在更早窗口返回空。
3. LLM alpha_analyst 编排非确定性（成功率约 6%），三次 formal 需多次重试。留待
   WP1-E3 统一处理。
4. PIT_SECTOR 无历史版本链。
