---
id: WP1-E4-R1
title: Locate full dynamic selector replay over accepted E2C and E3 research evidence
status: VERIFIED
risk: HIGH
owner: Claude
created_at: 2026-08-12T14:14:04+08:00
updated_at: 2026-08-13T14:34:09+08:00
allowed_files:
  - jiuwenswarm/evaluation/dynamic_selector_replay.py
  - jiuwenswarm/tests/unit_tests/quant/test_dynamic_selector_replay.py
  - coordination/active/WP1-E4-R1.md
acceptance:
  - Read-only location enumerates the accepted E2C replay, E3 fusion, registry, factor snapshot, maturity label, similar-market transformer and neighbor, selection, allocation, official 20-day outcome, Block Bootstrap, resource measurement, direct/formal isolation and focused-test boundaries.
  - Location proposes the smallest future write scope with exact KEEP / REPLACE / DELETE and does not edit product code, tests, facts, plans, production flags, direct/formal/RPC/E2E paths or credentials in this phase.
  - Location proves every historical decision date rebuilds its state from decision-time inputs only; later outcomes, later archive rows, whole-replay verdicts and future EvidenceRefs cannot affect the current policy or Agent input.
  - Location preserves one-shot A0/A1/A2 evaluation for the same outer decision and forbids using A0 or any outer outcome to decide whether A1/A2 runs.
  - Location maps negative coverage for price embargo, missing data, corporate actions, maturity, benchmark/neighbor insufficiency, proposal failure, selector leakage, hash drift, coverage and allocation mismatch, Block Bootstrap, resource gates and production isolation.
  - Any future result remains RESEARCH_ONLY; production_six_factor, overlay-disabled production behavior and the direct/formal three-role eight-RPC runtime remain unchanged.
---

## Goal

Perform read-only location for the smallest deterministic full dynamic selector replay boundary without changing production.

## Non-goals

- No implementation, source/test/archive/fact/plan edit, network access, model call, credential use, direct/formal/RPC/E2E run, commit, push, production promotion or report/submission claim.
- No change to E2C/E3 accepted verdicts, strategy definitions, official 20-day label semantics, stock selection, allocation constraints, runtime roles or the eight Quant RPCs.
- No use of price/volume matrices, ticker scores, weights, portfolios, backtests, future labels or whole-replay verdicts as LLM input or output authority.

## Invariants

- Preserve AGENTS.md, VALIDATION.md and DEVELOPMENT_PLAN.md evidence, causality, coverage and production contracts.
- Each decision independently rebuilds registry, PIT features, matured labels, transforms, neighbors, eligible candidates, bounded proposals, A0/A1/A2, selection/allocation and official outcome from hash-bound inputs.
- The official result is attached only after policy and portfolio identities are frozen; it never feeds the same decision or any earlier decision.
- The official 49-stock/6-sector coverage, exact selection-to-allocation identity, final stock/sector/cash assertions and corporate-action semantics must fail closed.
- Missing or invalid optional research branches fall back deterministically without reviving E2 failures; invalid A0 closes the decision rather than accepting Agent output.
- Replays must be deterministic, tamper-evident and independently recomputable; production remains unchanged.

## Locate brief

Use `.agents/skills/local-code-scout/SKILL.md`. Read only the minimum definitions, callers, tests and active contracts required for this task. Search before opening files and stay within the skill's read-call limit. Write only `output/agent_handoffs/WP1-E4-R1/location.json` and `claude_reply.md`; enumerate exact ranges, symbols, proposed future files, test commands, rollback and unresolved risks. Run only `python scripts/agent_task.py validate-location WP1-E4-R1`, then stop for Codex review. Implementation remains forbidden until Codex accepts the location and freezes a fresh baseline.

## Implementation evidence

- Codex location decision: `ACCEPT` with the following frozen choices.
- E2C is deterministically regenerated through the accepted public loader; no untracked artifact is trusted as the implementation oracle.
- Decision inventory must exactly equal the accepted E2C 12-window set. Any drift fails closed; it is not silently accepted as a new experiment.
- Each decision runs the accepted E3 boundary once with a caller-injected bounded model function. The standalone artifact command uses deterministic empty proposals only; no credential or real model call is permitted in this task.
- A0/A1/A2 share the same create-once bundle and are always evaluated. Their portfolio/outcome computation must be independent and must not use any variant outcome to gate another variant.
- Bootstrap parameters are frozen to `NestedEvaluationPlan`: seed `20260804`, iterations `2000`, block windows `3`; the payload binds these values and reports statistics without promoting production.
- Resource evidence for this research task is deterministic wall time plus process peak RSS measured for the local replay. It must be reported, not compared to formal token-reduction or formal P95 gates; missing/non-finite/negative resource fields fail verification.
- Research sanity ceilings are frozen at total monotonic elapsed `<= 1800` seconds and whole-replay process-tree peak RSS `<= 2048` MB; breach fails E4 artifact verification without implying a formal-path performance verdict.
- Exact write scope: KEEP all existing files read-only; ADD only `dynamic_selector_replay.py` and its focused test; REPLACE none; DELETE none. Task status file is the only other allowed change.

## Review evidence

- Location review `ACCEPT` by Codex. Confidence 0.80 is sufficient after independent interface checks. Implementation must include negative tests for decision-set drift, future-window mutation, whole-replay verdict mutation, hash tamper, one-shot call counts, variant independence, 49/6 coverage, selection/allocation identity, final constraints, Bootstrap binding, resource validation and production import isolation.

## Contract clarification v2 (2026-08-13)

- Codex accepts the reproducible exact-49 contract challenge. The authoritative decision-time
  source input must still cover exactly the official 49 stocks and 6 sectors and fail closed on
  any source-coverage loss.
- A slot composite is the eligible universe after the already accepted
  `compute_factors -> compute_scores -> filter_high_volatility` recipe. Every evaluated slot must
  expose exactly the same eligible ticker set; any asymmetric exclusion fails closed. Excluded
  official tickers and the resulting eligible set must be recorded and hash-bound per decision.
- A shared post-filter exclusion is not source-coverage loss and does not by itself fail the
  replay. This replaces only the superseded requirement that every slot composite contain all
  49 source tickers. It does not relax exact 49/6 source coverage, selection-to-allocation
  identity, final constraints, or tamper/future-data gates.
- A0 must remain byte/identity-equivalent to the accepted E2C production portfolio for the same
  decision. The composite recipe, production behavior, direct/formal paths and eight RPCs remain
  unchanged.
- Required regression evidence: exact 49/6 source loss fails; asymmetric slot exclusion fails;
  identical shared exclusions pass and are recorded/hash-bound; mutation of excluded/eligible
  ticker identities fails payload verification; A0 equality remains asserted.

## Progress

- 2026-08-12T14:14:04+08:00 `DRAFT`: Task created.
- 2026-08-12T14:25:11+08:00 `LOCATED`: Read-only location complete. `validate-location WP1-E4-R1` exit 0, confidence 0.80. Located E2C replay, E3 fusion, registry, factor snapshot, maturity label, similar-market transformer/neighbor, selection/allocation, official 20-day outcome, Block Bootstrap, resource measurement, direct/formal isolation and focused tests. Proposed minimal future write scope = NEW research-only module + NEW focused test + status file. Waiting for Codex review and baseline freeze.
- 2026-08-12T14:35:00+08:00 `LOCATED`: Codex location review ACCEPT. Frozen deterministic E2C regeneration, exact accepted 12-window inventory, injected/no-credential E3 calls, NestedEvaluationPlan Bootstrap parameters, local replay resource reporting and exact two-new-file implementation scope.
- 2026-08-12T14:27:29+08:00 `READY`: Baseline frozen; implementation may start.
- 2026-08-12T14:40:00+08:00 `REVIEWED`: Independent location review MODIFY supersedes the concurrent READY freeze. Missing slot-to-final-portfolio composition, research-safe resource measurement, and complete corporate-action boundaries. Implementation is not authorized.
- 2026-08-12T14:47:18+08:00 `LOCATED`: Location revised per `local-code-scout` to close the three review gaps: frozen slot->stock composition (blend per-slot composites into ONE production-sizer PositionSizer.allocate; A0 == production hard fallback; tie-break, excluded-slot, invalid-A0, fallback, selection==allocation, post-normalization constraints and negative tests enumerated); research-only wall-time/peak-RSS schema with formal aggregator demoted to isolation evidence (no 8/8-RPC/token gate) and proposed preregistered ceilings; corporate-action loader/provider/archive boundary with hashes, 294-receipt 49/6 coverage, same-day multi-action 8-tuple semantics and negative tests. Preserved frozen deterministic E2C regeneration, exact 12-window inventory and Bootstrap 20260804/2000/3. `validate-location WP1-E4-R1` pass pending; implementation remains not authorized until Codex accepts and freezes a fresh baseline.
- 2026-08-12T15:00:00+08:00 `REVIEWED`: Codex revision review MODIFY. Composition and corporate-action gaps are closed. Resource measurement still lacks a located sampler/lifecycle and incorrectly calls wall time deterministic; E2C oracle remains regeneration OR artifact read instead of one frozen mode. Prior baseline remains superseded; implementation is not authorized.
- 2026-08-12T15:12:00+08:00 `LOCATED`: Location revised again per `local-code-scout` to close the two remaining gaps: (1) resource meter frozen on the located `resource_meter.ProcessTreeRssSampler` (root+recursive-child RSS, interval 0.05s, start/stop lifecycle, running-max peak, missing/invalid-sample fail-closed) with monotonic elapsed via `time.monotonic()` and wall time observational only; per-window RSS removed (RSS is a whole-replay process-tree peak); formal aggregator stays demoted to isolation evidence. (2) E2C oracle frozen to the unique mode `load_e2c_evidence(regenerate=True)`; the artifact-read branch is removed from the contract. Composition, corporate-action, Bootstrap and production-isolation boundaries kept unchanged. `validate-location WP1-E4-R1` pass pending; implementation remains not authorized until Codex accepts and freezes a fresh baseline.
- 2026-08-12T14:49:42+08:00 `REVIEWED`: Codex ACCEPT location: composition, research-only resource meter, corporate-action boundary, exact 12-window oracle and Bootstrap choices frozen; fresh baseline required before implementation.
- 2026-08-12T14:49:44+08:00 `READY`: Baseline frozen; implementation may start.
- 2026-08-12T15:20:00+08:00 `REVIEWED`: Codex independently ACCEPTED the final revised location. ProcessTreeRssSampler lifecycle and observational monotonic elapsed boundary are located; E2C oracle is uniquely regenerate=True; prior composition/corporate-action/Bootstrap/isolation contracts remain closed. This is DESIGN_ONLY; any earlier baseline is stale and a separate fresh freeze is required before implementation.
- 2026-08-12T15:32:00+08:00 `READY`: Codex completed the separate bounded planning phase after the final location verdict. Frozen whitelist remains exactly the two new E4 files plus this task status file; research sanity ceilings are total monotonic elapsed <=1800s and whole-replay process-tree peak RSS <=2048MB. A fresh baseline and fresh scope-check are required by this phase; this authorizes only bounded implementation and does not raise evidence above DESIGN_ONLY.
- 2026-08-12T16:37:21+08:00 `READY`: Baseline frozen; implementation may start.
- 2026-08-12T16:55:00+08:00 `IMPLEMENTED`: Bounded implementation complete on the exact frozen whitelist (`dynamic_selector_replay.py` + `test_dynamic_selector_replay.py` + this status file). E2C oracle is uniquely `load_e2c_evidence(regenerate=True)`; per decision the module rebuilds the 251-session PIT window + operate corporate-action evidence, runs the E3 boundary once (create-once bundle, caller-injected no-credential model), evaluates A0/A1/A2 independently and composes each variant into a stock portfolio with ONE `PositionSizer` call; Block Bootstrap bound to `NestedEvaluationPlan` 20260804/2000/3; resource record uses `ProcessTreeRssSampler` + `time.monotonic()` and is validated fail-closed (`research_selector_resource/v1`, ceilings 1800s/2048MB). Evidence: focused pytest 36 passed, adjacent suite 109 passed, ruff/py_compile/`git diff --check` exit 0, `scope-check WP1-E4-R1` passed=true violations=[], standalone real replay exit 0 (artifact_sha256 `544b7799...`, identity_hash `457672ab...`, 12 windows, resource wall 204.36s / peak RSS 123.35MB, A0 == production E2C portfolio verified per window). RESEARCH_ONLY / LOCAL_IMPLEMENTED candidate; no production/direct/formal/RPC/E2E/commit/push. Handed to Codex for independent review.
- 2026-08-12T17:08:00+08:00 `REVIEWED`: Codex implementation review `MODIFY`. Scope-check and 36 focused tests pass, but the implementation retains a reachable `--no-regenerate` / `regenerate=False` artifact-read oracle forbidden by the frozen unique-oracle contract, and `_blend_frames` accepts identical 47-48-stock subsets although the frozen boundary requires each slot composite to cover all official 49 stocks. Resource whole-replay monotonic elapsed interpretation is accepted. Machine verdict is `output/agent_handoffs/WP1-E4-R1/review.json`; no production/path/business acceptance.
- 2026-08-13T09:48:00+08:00 `IMPLEMENTED`: MODIFY bounded fix applied within the frozen whitelist (two source/test files + this status file). (1) Removed the reachable artifact-read oracle: `run_selector_replay` no longer accepts a `regenerate` parameter and calls `load_e2c_evidence(regenerate=True)` literally; the `--no-regenerate` CLI switch is deleted; a new AST static regression `test_e2c_oracle_is_literal_regenerate_true_only` proves every executable `load_e2c_evidence` call passes literal `True` and the CLI defines no `--no-*` switch. (2) The exact-49 composite gate is submitted as an explicit versioned contract challenge, not a silent relaxation: on the real archive the frozen composite recipe (`compute_factors -> compute_scores -> filter_high_volatility`) drops the same 1-2 official names from both slots (verified 2025-01-14: 47 rows, dropping 601688.SH / 603986.SH), so the exact-49 gate and the frozen `A0 == production` invariant are mutually unsatisfiable on the real archive; the new test `test_exact_49_composite_gate_contract_challenge` reproduces the contradiction (both slots omit the same official ticker and `_blend_frames` accepts the identical filtered set) for Codex planning adjudication. Blend behavior unchanged, so no real ~200s replay re-run is required per the review. Evidence: focused pytest 38 passed, adjacent suite 109 passed, ruff / py_compile / `git diff --check` exit 0; `scope-check WP1-E4-R1` reports the single Codex-owned `.claude/discussion.md` violation (Codex appended the review handoff after the baseline freeze) and no other changed files outside the whitelist. Handed to Codex for the contract-challenge adjudication and baseline re-freeze; evidence remains `LOCAL_IMPLEMENTED / RESEARCH_ONLY`.
- 2026-08-13T10:10:00+08:00 `REVIEWED`: Codex independent implementation review `MODIFY`. The unique literal `load_e2c_evidence(regenerate=True)` oracle fix is accepted and focused pytest independently passes 38/38. The exact-49 contract challenge is accepted: source decision-time coverage remains exact 49 stocks/6 sectors, while post-`filter_high_volatility` eligible-slot ticker sets must be identical with exclusions recorded/hash-bound; A0==accepted E2C production remains unchanged. This clarification requires a separate versioned planning/fresh-baseline phase before acceptance. Current scope-check exits 2 with `.claude/discussion.md` and an additional unreported `启动双CLI协作.cmd` violation, so the present baseline cannot be accepted. No production/path/business acceptance.
- 2026-08-13T10:20:00+08:00 `REVIEWED`: Codex bounded review `BLOCKED` because the claimed fresh outbox contains no artifacts newer than the 10:10 MODIFY verdict and no subsequent IMPLEMENTED handoff or fresh baseline. Scope-check still exits 2 for `.claude/discussion.md` and `启动双CLI协作.cmd`. The 10:10 contract adjudication remains in force; the next phase must be separate Codex planning/fresh freeze, not another review of the stale diff.
- 2026-08-13T10:30:00+08:00 `REVIEWED`: Codex bounded review remains `BLOCKED`: the hook re-emitted the same stale outbox, with no implementation/reply/diff/baseline or IMPLEMENTED entry newer than the 10:20 verdict. Scope-check remains exit 2 for `.claude/discussion.md` and `启动双CLI协作.cmd`. No behavior review was repeated; next action remains separate Codex planning/fresh freeze.
- 2026-08-13T10:40:00+08:00 `REVIEWED`: Final repeated-block audit `BLOCKED`. This is the third consecutive hook turn with the identical pre-verdict outbox and unchanged scope-check violations (`.claude/discussion.md`, `启动双CLI协作.cmd`). Review cannot make meaningful progress until external state changes: separate Codex planning/fresh freeze followed by a new Claude `IMPLEMENTED` handoff. Repeated stale delivery now enters standby.
- 2026-08-13T10:50:00+08:00 `READY`: Codex completed exactly one separate bounded planning phase. Contract clarification v2 accepts the reproducible challenge: source coverage remains exact 49/6 fail-closed; post-filter slot eligible sets must be identical, with exclusions and eligible identities recorded/hash-bound; shared exclusions are allowed so A0 remains equal to accepted E2C production. Frozen write scope remains only the E4 source, focused test and this task file. No product behavior, production/direct/formal/RPC/E2E path or evidence level changed. A fresh baseline is required after this governance entry; `启动双CLI协作.cmd` is pre-existing user/local state and is incorporated into the baseline, not modified or claimed by this task.
- 2026-08-13T10:09:51+08:00 `READY`: Baseline frozen; implementation may start.
- 2026-08-13T10:09:52+08:00 `READY`: Contract clarification v2 frozen after current governance and local workspace state; bounded implementation authorized on exact whitelist only.
- 2026-08-13T14:16:33+08:00 `IMPLEMENTED`: Contract v2 bounded implementation on the exact frozen whitelist (`dynamic_selector_replay.py` + `test_dynamic_selector_replay.py` + this status file). The post-filter identical-eligible-set gate is explicit (`_blend_frames` raises "identical post-filter eligible ticker set" on asymmetric exclusion); per decision `decision_composition` records the loader-ticker `eligible_universe` and `excluded_stocks` (official 49 minus eligible) and `run_selector_replay` writes them into the window payload, so the excluded/eligible identities are bound into the per-window `window_hash` and tampering either list fails `verify_selector_payload`. Shared post-filter exclusions are accepted (real archive verified again on the fresh replay: every window drops the same `sh.601688` / `sh.603986` from both slots); the 49-stock/six-sector source frame, selection==allocation, final constraints and A0==accepted E2C production (per-window `_check_a0_matches_e2c_production`) remain asserted. E2C oracle stays the unique literal `load_e2c_evidence(regenerate=True)`; E3 create-once bundle, A0/A1/A2 independent evaluation, Bootstrap `20260804/2000/3` and resource ceilings 1800s/2048MB are unchanged. Negative coverage added/kept: source 49 loss and full-sector loss fail closed, asymmetric slot exclusion fails, identical shared exclusions pass and are recorded/hash-bound, excluded/eligible identity tamper fails verification, A0 inequality fails closed, hash drift / future-window mutation / one-shot call counts / Bootstrap binding / resource validation failure / production import isolation. Evidence: focused pytest 42 passed, adjacent suite 109 passed, ruff / py_compile / `git diff --check` (my files clean; the tracked `git diff --check` exit 2 is the pre-existing Codex-owned `.claude/discussion.md` blank-line-at-EOF) ; fresh real replay exit 0 (artifact_sha256 `d5c9727e...`, identity_hash `457672ab...` unchanged, 12 windows, resource wall 133.094s / peak RSS 125.6055MB / sample_count 1951, A0==production verified per window, `verify_selector_payload` True). Handed to Codex for independent review; evidence remains `LOCAL_IMPLEMENTED / RESEARCH_ONLY`.
- 2026-08-13T14:34:09+08:00 `VERIFIED`: Codex independent implementation review ACCEPT (2026-08-13). Contract v2 implemented: identical post-filter eligible-set gate, per-decision excluded/eligible records bound into window_hash, A0==E2C production asserted, unique literal load_e2c_evidence(regenerate=True) oracle. 42 focused tests + 109 adjacent tests pass; ruff/py_compile pass. scope-check fails solely on external Codex/bridge/user state (stale baseline); allowed task files are not violations. Evidence remains RESEARCH_ONLY / LOCAL_IMPLEMENTED.
