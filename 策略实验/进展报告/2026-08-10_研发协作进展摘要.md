# 2026-08-10 研发协作进展摘要

## 今日结论

今天主要完成了 E2 策略池后续数据可信链的任务拆解、范围冻结和三轮独立审查。
没有新增业务通过结论，没有联网抓取、正式路径运行、提交、推送或发布。

## 已完成工作

### 1. 对齐项目现状与协作职责

- 确认 Codex 负责计划、范围冻结、独立审查和验收裁决，Claude 负责只读定位、实现和验证。
- 确认当前事实源为 `VALIDATION.md`，路线与验收标准为 `DEVELOPMENT_PLAN.md`，双方交接使用 `.claude/discussion.md`。
- 保持正式金融运行时边界不变：Coordinator、Alpha Analyst、Risk & Evidence Analyst 三角色与 8 个 Quant RPC。

### 2. 审查 E2P/E2C 前置可信链

- 审查 WP1-E2P 的 qfq/PIT 补证，确认现有 corporate-action 归档使用 BaoStock `yearType='report'`，不能证明按 `dividOperateDate` 查询窗口的完整性。
- 裁决 WP1-E2P 继续 `BLOCKED`，因此依赖该可信桥的 WP1-E2C 确定性历史回放也继续 `BLOCKED`。
- 明确不能通过填伪 hash、放宽门禁或把 report-year 归档解释成 operate-year 证据来绕过阻塞。

### 3. 建立并冻结 WP1-E2O

- 新建 `WP1-E2O`，目标是建立 BaoStock `yearType='operate'` 的公司行动归档准入。
- 完成 Claude 只读定位、Codex 两轮定位审查和最终范围冻结。
- 冻结完整年份 `2020..2025`，官方 49 只股票，共 `49 × 6 = 294` 个请求回执；2026 部分年度不进入本任务。
- 冻结独立能力与信任身份：`PIT_CORPORATE_ACTION_OPERATE`、`BAOSTOCK_QUERY_DIVIDEND_DATA_OPERATE`、`corporate_action_operate_2020_2025/v1`。
- 冻结六文件白名单并生成 baseline；联网 BaoStock 抓取始终作为独立、未授权 gate。

### 4. 独立审查 Claude 离线实现

- 第一轮复审复现 4 个 P1：provider 信任绕过、任意 49 股票池自授权、归档双文件替换不安全、字段/日期/CSV 校验不足；裁决 `MODIFY`。
- 第二轮复审复现 3 个 P1：核心 builder 仍可自授权、receipt hash/CSV 投影仅表面校验、capability 未绑定 trusted key 与 inventory hash；裁决 `MODIFY`。
- Claude 修复第二轮问题后，最终复审确认原三项已修复，但又复现 3 个阻断缺陷：
  - CSV duplicate identity 未包含 ticker，跨股票相同动作会被误判重复；
  - receipt 深度校验接受 13 fields 下仅 6 列的截断 row；
  - generator 的 receipt/global duplicate_count 使用不同身份定义，可生成 provider 自己拒绝的归档。
- 按双方最多两次证据交换的治理规则，最终裁决 `REJECT`，不再在原 baseline 上开启第三轮 `MODIFY`。

## 当前状态

| 工作项 | 状态 | 说明 |
|---|---|---|
| WP1-E2A | CLOSED | 六槽位研究策略池注册表已验收 |
| WP1-E2B | CLOSED | prior-only 相似市场选择器已验收 |
| WP1-E2C | BLOCKED | 等待可信公司行动归档与 E2P 桥 |
| WP1-E2O | REVIEWED / REJECT | 离线实现存在 3 个 P1，联网 gate 未授权 |
| WP1-E2P | BLOCKED | report-year 证据不能替代 operate-year 完整性证明 |
| WP1-E2 总体 | BLOCKED | E2C 尚无真实历史回放与晋级证据 |

## 验证与证据边界

- 已用最小反例独立复现 WP1-E2O 最终三个 P1，并写入 `output/agent_handoffs/WP1-E2O/review.json`。
- 最终裁决和后续边界已写入 `.claude/discussion.md`。
- 项目虚拟环境启动器当前指向缺失的 Python 3.11，Codex 无法独立重跑 focused pytest；系统 Python 也未安装 pytest。
- `scope-check` 仅报告 Codex 在 baseline 后更新的 `.claude/discussion.md`；未发现 Claude 新增源代码越过冻结白名单。
- `VALIDATION.md` 的业务事实没有变化：最后接受的正式业务锚仍为 2026-08-07 的 v2.15 Windows formal `BUSINESS_PASSED`。

## 下一步建议

1. 新建版本化 WP1-E2O repair task，冻结新 baseline，不复用已 REJECT 的原任务继续修改。
2. 统一 generator、receipt、CSV 与 reconstruction 四处 duplicate identity，并补跨 ticker 与非 identity 字段差异测试。
3. 强制 receipt 每行宽度精确等于 fields 长度，覆盖短行和超长行负向测试。
4. 修复 Windows 项目虚拟环境后，由 Codex 独立重跑 focused pytest、Ruff、py_compile、diff-check 和 scope-check。
5. 离线实现验收通过后，再单独申请 BaoStock 联网授权，生成 294 个真实回执并钉住归档 hash；之后才能重启 E2P/E2C。

