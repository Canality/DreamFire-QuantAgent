# 2026-08-09 GitHub 发布前进展摘要

> 本摘要依据根目录 `VALIDATION.md` 记录的当前事实整理。本次 GitHub 源码发布前
> 整理产生了一次 fresh direct 路径证据，但没有 fresh formal 或 combined
> independent E2E 证据；本文不是正式投资分析报告，也不是比赛提交包。

## 本次 fresh 复验

- 2026-08-09 在 `jiuwenswarm` 工作目录运行
  `.\.venv\Scripts\python.exe -u scripts/run_quant_pipeline.py`，请求数据
  区间为 `2025-07-04 → 2026-08-08`，退出码为 `0`。
- direct 结果覆盖官方范围 49/49、6 个板块，形成 15 个持仓，现金权重 5.07%，
  quality 为 `PASSED`。
- 运行日志与产物为 `output/direct_github_release_20260809.log`、
  `output/pipeline_results_20260809_133817.json` 和
  `output/submission_candidates/direct-20260809_133817`（均相对仓库根目录）。
- 该运行仅达到 `PATH_PASSED`：direct 路径明确警告没有 Agent views，不能替代
  正式三角色与 8/8 Quant RPC 证据。
- fresh formal 与 combined independent E2E 均为 `Not tested`。当前 shell 没有
  安全的模型 credential，因此不复用或调用已经泄露的 key，也不降低正式门禁。

本次 fresh direct 不降低 2026-08-07 v2.15 formal `BUSINESS_PASSED` 历史锚，
也不把 `SubmissionContract` 从 `PROVISIONAL / BLOCKED` 提升为 `CONFIRMED`。

## 已完成实验与验收

- v2.15 Windows formal 已达到 `BUSINESS_PASSED`：三次运行均完成 8/8 Quant RPC，
  正常退出且三金融角色无越权；资源门的 P95、峰值 RSS 和 token 降幅均通过。
- WP1-D 已关闭：八阶段状态机、确定性 replay、资源聚合、正常 teardown 和失败
  关闭规则已完成 Windows 验收。
- WP1-E1P 五项研究数据能力均已准入：calendar、corporate action、E0 factor
  snapshot、forward label 和静态六板块 sector。
- WP1-B 已固化官方目标口径下的嵌套评测与晋级边界。WP1-C 的三个预注册
  challenger 均未通过晋级门，未继续搜索第四个候选。
- WP1-E0/E1 已形成 12 项 Factor Registry、稳定 implementation hash 和成熟标签
  因子研究框架，但仍保持 `RESEARCH_ONLY`，没有接入生产策略。

上述证据等级、commit、session 和资源数值以
[`VALIDATION.md`](../../VALIDATION.md) 为唯一事实源。

## 当前最优策略

当前唯一具有生产资格和硬回退地位的策略仍是 `production_six_factor`。T2 仅作
`RESEARCH_ONLY` 对照；三个 WP1-C challenger 均为 `DOES_NOT_QUALIFY`；Agent
overlay 继续关闭。因此，本次 GitHub 发布不调整因子方向、权重、选股规则、仓位
约束或生产指针，也不根据旧的受污染本地分数推断比赛收益。

## 待验证方向

1. 按 WP1-E2 → E3 → E4 的顺序实现并验证六槽位策略池、有界 Agent 融合和完整
   selector replay；任何结果先形成新的 `RESEARCH_ONLY` 证据。
2. fundamental 与 news-risk 只有在获得合法、可归档且可跨设备交付的数据授权后
   才恢复 Provider 工作；在此之前报告保持 `FINANCIAL_PARTIAL`。
3. 获取主办方对官方范围 49/50、现金权重口径和报告作用的可归档书面答复，再建立
   contract version/hash 并启动 WP2。
4. 最终提交前在最终 commit、最终数据和 Windows 环境重新运行 direct、formal 与
   独立 E2E 审计，不复用本次文档整理作为运行证据。

## GitHub 发布边界

- 公开默认模型配置只保留 `${API_KEY}` 环境变量占位符；本机 `.codex/` 与
  `codex-opencode-go.*` 启动文件不进入版本控制。
- 已经暴露过的凭据仍需在外部完成轮换或吊销；修改当前文件不能撤销历史暴露。
- `SubmissionContract` 继续为 `PROVISIONAL / BLOCKED`。本次发布仅整理源码仓库，
  不生成或命名正式比赛提交包。
