# 2026-08-09 比赛研发进展摘要

## 今日结论

今天完成了 GitHub 发布整理、策略池两个研究基础模块，以及真实历史回放前的信任链
排障；尚未产出新的候选策略收益结果。当前最优且唯一 production-qualified 策略仍为
`production_six_factor`，没有候选晋级，也没有修改 production。

## 已完成

1. **GitHub 发布链已对齐**
   - `master` 与 `origin/master` 当前均为 `46c8460`（`chore: prepare public GitHub release`）。
   - 当前配置已改用 `${API_KEY}`，本机 `.codex/config.toml` 与
     `codex-opencode-go.cmd/.ps1` 已排除，不进入仓库。
   - 2026-08-09 fresh direct：请求日期 `2025-07-04 → 2026-08-08`，exit 0，覆盖
     49/49 公司、6 板块、15 持仓、cash 5.07%、quality `PASSED`。由于 direct 路径
     无 Agent views，证据等级严格保持 `PATH_PASSED`。
   - 已知安全遗留：旧实值 key 自初始提交起存在于公共 Git 历史；当前 HEAD 脱敏不能
     替代外部轮换/吊销。

2. **WP1-E2A 六槽位研究策略注册表完成并验收**
   - 固化 production、T2、短/中/长期趋势和相似市场六个槽位。
   - production 仍是唯一硬回退；研究模块未接入 direct/formal/production。
   - Codex 独立结论 `ACCEPT / CLOSED`；聚焦测试 13 passed，Ruff、py_compile、
     diff-check、scope-check 均通过。

3. **WP1-E2B prior-only 相似市场选择器完成并验收**
   - 使用六维特征、仅历史成熟状态、median/MAD、非重叠邻居和确定性排序。
   - 真实对齐 benchmark 仍不存在，因此真实数据路径按契约保持
     `BENCHMARK_UNAVAILABLE`，没有伪造相似市场结果。
   - Codex 独立结论 `ACCEPT / CLOSED`；聚焦测试 37 passed，静态与范围检查通过。

4. **WP1-E2C 真实策略池回放已完成实验契约设计，但尚未实现**
   - 已预注册相同窗口、49 股/6 板块、1-day embargo、20-session 持有期、统一配仓
     约束和候选晋级阈值。
   - 实现前发现真实归档无法通过 typed factor input，因此任务已正确设为 `BLOCKED`，
     没有用私有 factor kernels 绕过信任门。

5. **WP1-E2P 真实归档信任桥完成只读定位**
   - 精确发现 3 个受 Windows CRLF 影响的 pinned 文件；Git blob 是准入的 LF 字节，
     当前 worktree CRLF 导致 readiness 失败。
   - 找到 calendar id、corporate-action authority 两处契约冲突。
   - 找到 corporate-action 的双哈希语义冲突：归档 CSV SHA-256 用于 provider 准入，
     typed instance hash 用于绑定 policy/window/ticker results 和 snapshot 派生链。
   - 已确认修复后存在 626 个 confirmed sessions，前 271 个由 49 股 qfq 完整覆盖，
     技术上可构造首个 251-session 输入并通过公开 API 计算 49×12 因子快照。
   - 当前状态 `LOCATED`，尚未冻结、尚未实现。

## 当前事实与限制

- 当前策略：`production_six_factor`；今日没有新回测结论，不能声称策略收益提升。
- E2A/E2B：`LOCAL_IMPLEMENTED / RESEARCH_ONLY`。
- E2C：被 E2P 信任契约阻塞。
- E3/E4：未开始，必须等待 E2C 给出真实的相同窗口比较证据。
- `SubmissionContract` 仍为 `PROVISIONAL / BLOCKED`；49/50、现金口径和报告权重仍需
  主办方书面确认。
- fresh formal 与 combined independent E2E 今日未运行；历史 v2.15 Windows formal
  `BUSINESS_PASSED` 锚点不变。

## 明天的第一步

Codex 先完成 WP1-E2P 冻结前最后审查：明确 corporate-action typed evidence 必须按
**每个决策窗口**绑定，并核验当前 qfq 数据在历史决策时点的调整价因果语义，不能仅凭
“qfq”名称声明 point-in-time safe。若证据成立，冻结以下最小范围及 baseline：

- `.gitattributes`（仅 3 个精确 `eol=lf` 路径）；
- `candidate_factors.py`；
- 新公开 `research_evidence_loader.py`；
- `test_candidate_factors.py`；
- 新 `test_research_evidence_loader.py`。

provider trust root、E2C replay、direct/formal、production 和 `quant/__init__.py` 不解冻。

## 后续顺序

1. Claude 在冻结范围内实现 WP1-E2P，跑真实归档 251-session、49×12 集成测试和全部
   负向门禁，提交 `implementation.json`。
2. Codex 独立审查差异、信任边界、Windows LF checkout 和测试；仅在 `ACCEPT` 后关闭
   E2P 并重新打开 E2C。
3. Claude 实现 E2C，实际运行 pinned-data 策略池历史回放。这一步才会首次产出短/中/
   长趋势、T2 相对 production 的真实收益、回撤和 paired utility 证据。
4. Codex 按预注册阈值裁决：不达标即 `DOES_NOT_QUALIFY`；只有达标候选才进入 E3
   有界 Agent 融合，之后再进入 E4 outer selector replay。

明日不以增加模块数量为目标，优先打通“真实归档 → 公共因子 API → 相同窗口回放”这条
可核验链路，并尽快得到对比赛策略是否有帮助的真实结果。
