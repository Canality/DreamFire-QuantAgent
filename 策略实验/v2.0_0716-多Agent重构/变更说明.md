# v2.0 变更说明 (2026-07-16)

## 从 v1.0 到 v2.0 的核心变化

### 架构重构
- 策略代码从独立 `solution/` 迁移到 `jiuwenswarm/quant/` 包
- 创建 `QuantFinanceExtension` (8个 RPC Handler)
- 创建 `QuantToolkit` (8个 Agent 可调用工具)
- 注册为 Swarm Provider

### 多 Agent 协作系统
- Coordinator (Quant PM) + Bull Analyst (多头) + Bear Analyst (风控)
- config.yaml 新增 `quant_team` 模式
- Bull/Bear 并行分析 → Coordinator 综合裁决

### 策略改进
- Bull 和 Bear 使用不同的 FactorConfig 权重
- 评分阈值从硬编码改为分位数
- 删除所有模拟数据降级

### 评分: 76.9/100
